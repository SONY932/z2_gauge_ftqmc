# 数值问题修复总结

## 问题分析

### 问题1：lambda投影导致的段栈不一致

**原因**：
- 在`local_sweep`中，lambda投影直接修改了`Gr`，但段栈（UUR/UUL/VUR/VUL/DUR/DUL）没有相应更新
- 即使通过再次调用`apply_lambda_both`来撤回投影，由于浮点误差，`Gr`与段栈可能不完全一致
- 如果lambda更新被接受，`Gauge%lambda`已经改变，撤回投影时使用的是新的lambda值，无法正确恢复

**修复**（`src/local_sweep.f90`）：
- 在应用lambda投影之前，保存`Gr`的副本（第228-231行）
- 无论lambda更新是否被接受，都恢复保存的`Gr`副本（第252-253行）
- 如果lambda更新被接受，重建段栈以确保与新的规范场一致（第260-261行）

### 问题2：稳定化中的数值保护不足

**原因**：
- 在`stab_green`中除以`DUP`时，如果`DUP`过小（接近1d-300），可能导致数值溢出或精度损失

**修复**（`src/stabilization.f90`）：
- 在除以`DUP`之前检查其大小（第219行和第248行）
- 如果`DUP`过小（< 1d-200），将对应元素置零，避免数值问题

## 修复效果

这些修复应该能解决：

1. **即使不开启lambda更新，程序也能正常工作**
   - 通过保存和恢复`Gr`副本，避免了浮点误差累积导致的段栈不一致

2. **开启lambda更新后，段栈能正确重建**
   - 如果lambda更新被接受，重建段栈以确保与新的规范场一致
   - 如果lambda更新被拒绝，恢复`Gr`副本以保持段栈一致性

3. **数值稳定性问题得到改善**
   - 在稳定化计算中添加了数值保护，避免除以过小的值

## 修改的文件

1. `src/local_sweep.f90`：修复lambda投影导致的段栈不一致
2. `src/stabilization.f90`：添加稳定化中的数值保护

## 测试建议

1. 编译程序（需要完整的编译环境）
2. 运行测试用例（`test/2-1/a/L10/h0.52/`）
3. 检查日志文件，确认没有NaN或数值不稳定警告
4. 验证观测量的合理性

## 注意事项

- 修复后的代码保持了原有的逻辑结构，只是添加了必要的保护措施
- 如果lambda更新被接受，段栈会重建，这可能会稍微增加计算时间，但确保了正确性
- 数值保护的阈值（1d-200）可以根据实际情况调整
